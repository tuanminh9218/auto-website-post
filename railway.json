{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
          "startCommand": "export LD_LIBRARY_PATH=$(find /nix/store -maxdepth 3 -name \"libstdc++.so.6\" | head -n 1 | xargs dirname):$LD_LIBRARY_PATH && cd backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
