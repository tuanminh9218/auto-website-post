{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
          "startCommand": "for f in $(find /nix/store -maxdepth 3 -name \"libstdc++.so.6\"); do if ! grep -q \"GLIBC_2.38\" \"$f\"; then export LD_LIBRARY_PATH=$(dirname \"$f\"):$LD_LIBRARY_PATH; break; fi; done && cd backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
